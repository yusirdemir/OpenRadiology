{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Native folder picking and opening a produced report in the system viewer. Nothing else is granted: the sidecar is spawned by Rust, not by the web view, so the front end never gets shell access.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:allow-open",
    "dialog:allow-message",
    "opener:allow-open-path"
  ]
}
